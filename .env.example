# Copy to .env and fill in. Never commit .env (see .gitignore).

# Google ADK / Vertex AI
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=
GOOGLE_GENAI_USE_VERTEXAI=true

# HCM system credentials (mcp_server/)
HCM_API_BASE_URL=
HCM_API_KEY=
